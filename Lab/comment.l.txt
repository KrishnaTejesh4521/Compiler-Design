%{
#include<stdio.h>
int nc = 0;
%}

%%

"/*"(.|\n)*?"*/"   { nc++; }
"//".*             { nc++; }

.|\n               ;

%%

int yywrap()
{
    return 1;
}

int main(int argc, char *argv[])
{
    yyin = fopen(argv[1], "r");
    yylex();
    printf("Number of comments = %d\n", nc);
    return 0;
}