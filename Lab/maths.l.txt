%{
#include<stdio.h>
int a,b;
%}

%%

[0-9]+[\+][0-9]+ { sscanf(yytext,"%d+%d",&a,&b);
                   printf("Sum=%d\n",a+b); }

[0-9]+[-][0-9]+ { sscanf(yytext,"%d-%d",&a,&b);
                  printf("Difference=%d\n",a-b); }

[0-9]+[\*][0-9]+ { sscanf(yytext,"%d*%d",&a,&b);
                   printf("Product=%d\n",a*b); }

[0-9]+[/][0-9]+ { sscanf(yytext,"%d/%d",&a,&b);
                  printf("Quotient=%d\n",a/b); }

%%

int yywrap()
{
return 1;
}

int main()
{
yylex();
}